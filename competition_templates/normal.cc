#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#ifdef LOL_DEBUG
#include debug_path
#else
#define dbg(...) 101
#endif
const char df = '\n';
void solve() {
    
}
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int tt = 1;
    cin >> tt;
    for (int i = 1; i <= tt; i++) {
        solve();
    }
    return 0;
}