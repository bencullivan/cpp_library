#include <bits/stdc++.h>

typedef long long ll;
using namespace std;

void solve() {
}

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0);
    int tt = 1;
    cin >> tt;
    for (int i = 0; i < tt; i++) {
        cout << "Case #" << i+1 << ": ", solve();
    }
    return 0;
}