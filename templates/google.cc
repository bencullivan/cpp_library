#include <bits/stdc++.h>
#define sz(v) int64_t(v.size())
using namespace std;
using ll=long long;

void solve_one() {
}

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    for (int i = 1; i <= t; i++) {
        cout << "Case #" << i << ": ";
        solve_one();
    }
}