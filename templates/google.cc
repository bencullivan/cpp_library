#include <bits/stdc++.h>
using namespace std;
using ll=long long;
#define f first
#define s second
#define pb push_back
#define all(v) v.begin(),v.end()
#define sz(v) int64_t(v.size())

void solve_one() {
}
int main() {
  ios_base::sync_with_stdio(0), cin.tie(0);
  int t;
  cin >> t;
  for (int i = 1; i <= t; i++) {
    cout << "Case #" << i << ": ";
    solve_one();
  }
}