#include <bits/stdc++.h>
using namespace std;
using ll=long long;
using pii=pair<int,int>;
using pll=pair<ll,ll>;
using vi=vector<int>;
using vvi=vector<vi>;
using vl=vector<ll>;
using vvl=vector<vl>;
using vb=vector<bool>;
using vvb=vector<vb>;
using vd=vector<double>;
using vvd=vector<vd>;
using vs=vector<string>;
mt19937 irng(chrono::system_clock::now().time_since_epoch().count());
#define f first
#define s second
#define pb push_back
#define rep(i,a,b) for(int i=a;i<(b);++i)
#define all(v) v.begin(),v.end()
#define sz(v) int64_t(v.size())

void solve_one() {
}
int main() {
  ios_base::sync_with_stdio(0), cin.tie(0);
  int t;
  cin >> t;
  for (int i = 1; i <= t; i++) {
    cout << "Case #" << i << ": ";
    solve_one();
  }
}