#include <bits/stdc++.h>

typedef long long ll;
typedef unsigned long long ull;
using namespace std;

void solve() {
}

signed main() {
	ios_base::sync_with_stdio(0), cin.tie(0);
	int tt = 1;
	cin >> tt;
	for (int i = 1; i <= tt; i++) {
		cout << "Case #" << i << ": ";
		solve();
	}
	return 0;
}