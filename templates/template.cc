#include <bits/stdc++.h>
#ifndef LOCAL
#define dbg(...) 0
#define dba(...) 0
#endif
using namespace std;

void solve_one() {
}

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve_one();
}